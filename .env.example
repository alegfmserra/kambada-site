# =============================================================================
# Kambada — variáveis de ambiente
# Copie para .env.local e preencha. .env.local NUNCA vai para o repositório.
# =============================================================================

# --- Site ---------------------------------------------------------------
NEXT_PUBLIC_SITE_URL="https://somoskambada.com.br"

# --- Bling --------------------------------------------------------------
# Gerados em: Bling > Preferências > Integrações > API > criar aplicativo.
# Só server-side. Nunca prefixar com NEXT_PUBLIC_.
BLING_CLIENT_ID=""
BLING_CLIENT_SECRET=""

# Deve bater EXATAMENTE com a URL cadastrada no aplicativo do Bling.
# Em desenvolvimento: http://localhost:3000/api/bling/callback
BLING_REDIRECT_URI="https://somoskambada.com.br/api/bling/callback"

# Segredo que NÓS inventamos (não vem do Bling) e cadastramos no webhook do
# Bling. Sem ele configurado, o webhook recusa tudo — falha fechada.
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BLING_WEBHOOK_SECRET=""

# Onde os tokens do Bling ficam gravados. Precisa ser um caminho que sobreviva
# a reinício e a deploy — se o arquivo sumir, é preciso autorizar de novo.
# NUNCA versionar: o refresh_token dá acesso ao ERP inteiro.
BLING_TOKENS_ARQUIVO=".dados/bling-tokens.json"

# --- Mercado Pago (Fase 3) ----------------------------------------------
MERCADOPAGO_ACCESS_TOKEN=""
NEXT_PUBLIC_MERCADOPAGO_PUBLIC_KEY=""
MERCADOPAGO_WEBHOOK_SECRET=""

# --- Revalidação de cache -----------------------------------------------
# Protege a rota que invalida o cache do catálogo. Gere um valor aleatório longo.
REVALIDATE_SECRET=""
